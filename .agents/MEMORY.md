# Project Knowledge Memory Index

This file tracks non-obvious architecture rules, environment quirks, and hard-learned lessons. Update this index whenever a major engineering task concludes.

## Deep-Dive Skill Guides
- See `.agents/skills/type-hints/SKILL.md` when working with `as ()`/`as {}` shape hints, `child_parents` field-path notation, or the `child_parents` `into:`/`from:` action syntax.
- See `.agents/skills/test-writing/SKILL.md` when writing/extending o2o-tests integration tests (naming conventions, fast iteration, the `--no-default-features` feature-flag gotcha, and the attribute-renaming table for deriving a `_fallible` counterpart file).
- See `.agents/skills/child-instructions/SKILL.md` when dealing with 'asymmetrically' nested structs.